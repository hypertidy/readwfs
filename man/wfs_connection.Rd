% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{wfs_connection}
\alias{wfs_connection}
\title{Build a GDAL OGR connection for a vector web service}
\usage{
wfs_connection(
  base_url,
  driver = c("auto", "WFS", "OAPIF", "ESRIJSON"),
  version = NULL,
  srs = NULL
)
}
\arguments{
\item{base_url}{Character. The service endpoint URL.}

\item{driver}{Character. One of \code{"auto"}, \code{"WFS"}, \code{"OAPIF"}, \code{"ESRIJSON"}.
When \code{"auto"}, the driver is inferred from the URL pattern.}

\item{version}{Character or \code{NULL}. WFS version (e.g. \code{"2.0.0"}). Ignored
for non-WFS drivers.}

\item{srs}{Character or \code{NULL}. Target SRS as \code{"EPSG:XXXX"}. Passed as
\code{srsName} for WFS.}
}
\value{
A list with \code{dsn} (GDAL connection string) and \code{driver} (resolved
driver name).
}
\description{
Constructs the appropriate GDAL connection string from a base URL,
auto-detecting the service type (WFS, OAPIF, ArcGIS REST) or using
an explicit driver hint.
}
\keyword{internal}
