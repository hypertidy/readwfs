% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{wfs_read}
\alias{wfs_read}
\title{Read features from a vector web service}
\usage{
wfs_read(
  base_url,
  layer,
  bbox = NULL,
  max_features = NULL,
  where = NULL,
  driver = "auto",
  version = NULL,
  srs = NULL,
  convert_linear = TRUE,
  promote_to_multi = FALSE
)
}
\arguments{
\item{base_url}{Character. The service endpoint URL.}

\item{layer}{Character. Layer name to read. Use \code{\link[=wfs_layers]{wfs_layers()}} to
discover available layers.}

\item{bbox}{Numeric vector of length 4 (\code{xmin}, \code{ymin}, \code{xmax}, \code{ymax})
or \code{NULL} for no spatial filter. Coordinates should be in the SRS
specified by \code{srs} (or the layer's native SRS if \code{srs} is \code{NULL}).}

\item{max_features}{Integer or \code{NULL}. Maximum number of features to
request. For WFS, passed as \code{count} in the URL. For other drivers,
features are truncated after fetch.}

\item{where}{Character or \code{NULL}. An OGR SQL \code{WHERE} clause to filter
features by attribute.}

\item{driver}{Character. Driver hint; see \code{\link[=wfs_connection]{wfs_connection()}}.}

\item{version}{Character or \code{NULL}. WFS version (e.g. \code{"2.0.0"}).}

\item{srs}{Character or \code{NULL}. Target SRS as \code{"EPSG:XXXX"}.}

\item{convert_linear}{Logical. If \code{TRUE} (default), convert
circular arc geometries to linear approximations.}

\item{promote_to_multi}{Logical. If \code{TRUE}, promote single-part
geometries to multi-part. Default \code{FALSE}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with attribute columns and a \code{geometry}
column of class \code{wk::wkb}.
}
\description{
Fetches vector features from a WFS, OGC API Features, or ArcGIS REST
endpoint and returns a tibble with a \code{wk::wkb} geometry column.
}
\examples{
\dontrun{
# Tasmania LIST: cadastral parcels in Sandy Bay
parcels <- wfs_read(
  wfs_example_url("list_tasmania"),
  layer = "Public_OpenDataWFS:LIST_CADASTRAL_PARCELS",
  bbox = wfs_example_bbox("sandy_bay"),
  srs = "EPSG:28355",
  max_features = 100
)
parcels
wk::wk_plot(parcels$geometry)

# Esri sample world cities
cities <- wfs_read(
  wfs_example_url("esri_sample"),
  layer = "esri:cities"
)
}
}
