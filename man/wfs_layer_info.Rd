% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{wfs_layer_info}
\alias{wfs_layer_info}
\title{Get layer metadata from a vector web service}
\usage{
wfs_layer_info(
  base_url,
  layers = NULL,
  driver = "auto",
  version = NULL,
  srs = NULL
)
}
\arguments{
\item{base_url}{Character. The service endpoint URL. Can be a raw URL
or a GDAL-prefixed connection string (e.g. \code{"WFS:https://..."}).}

\item{layers}{Character vector of layer names to inspect, or \code{NULL}
to inspect all layers. Inspecting all layers can be slow for large
services -- consider using \code{\link[=wfs_find_layers]{wfs_find_layers()}} first.}

\item{driver}{Character. One of \code{"auto"}, \code{"WFS"}, \code{"OAPIF"}, \code{"ESRIJSON"}.}

\item{version}{Character or \code{NULL}. WFS version.}

\item{srs}{Character or \code{NULL}. Target SRS.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns: \code{name}, \code{geom_column},
\code{geom_type}, \code{feature_count}, \code{xmin}, \code{ymin}, \code{xmax}, \code{ymax}, \code{srs_wkt}.
}
\description{
Returns a tibble describing each layer: geometry type, feature count
(if available), spatial extent, and spatial reference.
}
\examples{
\dontrun{
url <- wfs_example_url("list_tasmania")
wfs_layer_info(url, layers = "Public_OpenDataWFS:LIST_Cadastral_Parcels",
               version = "2.0.0", srs = "EPSG:28355")
}
}
